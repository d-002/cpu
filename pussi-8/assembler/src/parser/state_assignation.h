#ifndef STATE_ASSIGNATION_H
#define STATE_ASSIGNATION_H

#include "mystring.h"
#include "state.h"

int state_assignation(struct state *state, struct string *string);

#endif /* ! STATE_ASSIGNATION_H */
