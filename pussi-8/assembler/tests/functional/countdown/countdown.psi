._test_label_at_index_0
    ldi start,%r1
    ldi decrement_by,%r2

.loop:
    sub %r1,%r2
    movei %r0,%r1
    out %r1,port

    jnz loop
    halt

start = 10
decrement_by = 1
port = %p0 ; this is a constructive comment for testing

; this is another comment, also for testing ;
